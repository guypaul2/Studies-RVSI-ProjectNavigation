using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EcoEffect : MonoBehaviour
{
    // Start is called before the first frame update
    
    public void Effect()
    {
        Debug.Log("Effect is" + gameObject.name);
    }
}
